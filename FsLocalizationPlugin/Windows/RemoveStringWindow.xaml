<ctrl:FrostyDockableWindow x:Class="FsLocalizationPlugin.Windows.RemoveStringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FsLocalizationPlugin.Windows"
        xmlns:core="clr-namespace:Frosty.Core;assembly=FrostyCore"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:fdev="FrostyDeveloper"
        mc:Ignorable="d"
        UseLayoutRounding="True"
        Title="Remove String - Flammenwerfer" SizeToContent="Height" MaxHeight="850" Width="400"
        x:Name="removeStringWindow"
        WindowStartupLocation="CenterOwner"
                           AllowDrop="True"
        >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource ListBackground}"/>
        <StackPanel x:Name="MainStackPanel" Visibility="Visible" Grid.Row="0" Margin="5, 10, 5, 10">
            <WrapPanel Orientation="Horizontal" Margin="3">
                <Label Width="Auto">
                    <Label.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Database.png"
                    RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Binary Strings: "/>
                        </StackPanel>
                    </Label.Content>
                </Label>
                <Button Padding="6, 3, 6, 3" Margin="0, 2, 0, 2" Width="Auto" Click="BrowsePathButton_Click" Tag="Binary">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Open.png"
                    RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Browse"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Label x:Name="BinaryDragTips" Width="Auto">
                    <Label.Content>
                        <TextBlock Foreground="{StaticResource FontColor}" Text="or drag file here"/>
                    </Label.Content>
                </Label>
                <TextBox Padding="2.5" Margin="2" Width="Auto"
        BorderThickness="1"
        Text="{Binding BinaryFilePath, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
        AcceptsTab="False"
        AcceptsReturn="False"
        AutoWordSelection="False"
        VerticalScrollBarVisibility="Disabled" 
        HorizontalScrollBarVisibility="Disabled"
        >
                </TextBox>
            </WrapPanel>

            <WrapPanel Orientation="Horizontal" Margin="3">
                <Label Width="Auto">
                    <Label.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Grid.png"
        RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Histogram: "/>
                        </StackPanel>
                    </Label.Content>
                </Label>
                <Button Padding="6, 3, 6, 3" Margin="0, 2, 0, 2" Width="Auto" Click="BrowsePathButton_Click" Tag="Histogram">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Open.png"
        RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Browse"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
                <Label x:Name="HistogramDragTips" Width="Auto">
                    <Label.Content>
                        <TextBlock Foreground="{StaticResource FontColor}" Text="or drag file here"/>
                    </Label.Content>
                </Label>
                <TextBox Padding="2.5" Margin="2" Width="Auto"
                    BorderThickness="1"
                    Text="{Binding HistogramFilePath, RelativeSource={RelativeSource AncestorType=Window}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    AcceptsTab="False"
                    AcceptsReturn="False"
                    AutoWordSelection="False"
                    VerticalScrollBarVisibility="Disabled" 
                    HorizontalScrollBarVisibility="Disabled"
                    >
                </TextBox>
            </WrapPanel>

            <WrapPanel Orientation="Horizontal" Margin="3">
                <Label Width="Auto">
                    <Label.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="0, 1, 5, 0" Text="&#xF2B7;" 
                                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Language: "/>
                        </StackPanel>
                    </Label.Content>
                </Label>
                <ComboBox x:Name="LanguageComboBox" MinWidth="140" Width="Auto" Margin="2">
                </ComboBox>
            </WrapPanel>

            <StackPanel Margin="3" Orientation="Vertical">
                <Label Width="Auto">
                    <Label.Content>
                        <StackPanel Orientation="Horizontal">
                            <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyCore;component/Images/Settings.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Foreground="{StaticResource FontColor}" Text="Options: "/>
                        </StackPanel>
                    </Label.Content>
                </Label>

                <WrapPanel Orientation="Vertical" Margin="15, 0, 0, 0">
                    <WrapPanel Orientation="Horizontal" Margin="3, 0, 3, 0">
                        <Label Width="Auto">
                            <Label.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Foreground="{StaticResource FontColor}" Text="Do not import empty or null strings: "/>
                                </StackPanel>
                            </Label.Content>
                        </Label>
                        <CheckBox x:Name="NoImportEmptyOrNullCheckBox" Margin="0, 2, 0, 2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </WrapPanel>

                    <WrapPanel Orientation="Horizontal" Margin="3, 0, 3, 0">
                        <Label Width="Auto">
                            <Label.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Foreground="{StaticResource FontColor}" Text="Delete all existing strings: "/>
                                </StackPanel>
                            </Label.Content>
                        </Label>
                        <CheckBox x:Name="DeleteExistStringsCheckBox" Margin="0, 2, 0, 2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </WrapPanel>

                    <WrapPanel Orientation="Horizontal" Margin="3, 0, 3, 0" IsEnabled="True">
                        <WrapPanel.Style>
                            <Style TargetType="WrapPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=DeleteExistStringsCheckBox, Path=IsChecked}" Value="False">
                                        <Setter Property="IsEnabled" Value="True"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ElementName=DeleteExistStringsCheckBox, Path=IsChecked}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </WrapPanel.Style>
                        <Label Width="Auto">
                            <Label.Content>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Foreground="{StaticResource FontColor}" Text="Do not overwrite same existing strings: "/>
                                </StackPanel>
                            </Label.Content>
                        </Label>
                        <CheckBox x:Name="NoOverwriteSameStringsCheckBox"
                                  Margin="0, 2, 0, 2"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center">
                        </CheckBox>
                    </WrapPanel>
                </WrapPanel>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1" Height="38">
            <Grid Margin="5">
                <DockPanel LastChildFill="False">
                    <Button x:Name="CancelButton" Padding="6, 3, 6, 3" Margin="3" DockPanel.Dock="Left" Width="Auto" Click="CancelButton_Click">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyControls;component/Images/Clear.png"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Foreground="{StaticResource FontColor}" Text="Cancel"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button x:Name="ConfirmButton" Padding="6, 3, 6, 3" Margin="3" DockPanel.Dock="Right" Width="Auto" Click="ConfirmButton_Click" 
                            IsEnabled="{Binding CanImport}">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Import.png"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Foreground="{StaticResource FontColor}" Text="Import"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button x:Name="StartOverButton" Padding="6, 3, 6, 3" Margin="3" DockPanel.Dock="Right" Width="Auto" Click="StartOverButton_Click">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="0, 1, 5, 0" Text="&#xe72c;" 
                                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Foreground="{StaticResource FontColor}" Text="Start Over"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                </DockPanel>
            </Grid>
        </Grid>

    </Grid>
</ctrl:FrostyDockableWindow>
