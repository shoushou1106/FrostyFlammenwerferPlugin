<local:FlammenwerferWindowBase x:Class="FsLocalizationPlugin.Windows.ImportChunksWindow"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:local="clr-namespace:FsLocalizationPlugin.Windows"
                               xmlns:core="clr-namespace:Frosty.Core;assembly=FrostyCore"
                               xmlns:fdev="FrostyDeveloper"
                               mc:Ignorable="d"
                               UseLayoutRounding="True"
                               Title="Import Chunks from Files - Flammenwerfer" SizeToContent="Height" MaxHeight="850" Width="400"
                               AllowDrop="True"
                               DragEnter="Window_DragEnter"
                               DragLeave="Window_DragLeave"
                               FontFamily="Segoe UI Variable Text, Global User Interface">
  <!-- View model: ViewModels/ImportChunksViewModel.cs -->

  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <Grid Background="{StaticResource WindowBackground}">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="{StaticResource ListBackground}"/>
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel x:Name="MainStackPanel" Margin="5, 10, 5, 10">
        <WrapPanel Orientation="Horizontal" Margin="3">
          <Label Target="{Binding ElementName=BinaryBrowseButton}">
            <StackPanel Orientation="Horizontal">
              <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Database.png"
                     RenderOptions.BitmapScalingMode="HighQuality"/>
              <AccessText Foreground="{StaticResource FontColor}" Text="Binary _Strings: "/>
            </StackPanel>
          </Label>
          <Button x:Name="BinaryBrowseButton" Padding="6, 3, 6, 3" Margin="0, 2, 0, 2" Command="{Binding BrowseCommand}" CommandParameter="Binary">
            <Button.Content>
              <StackPanel Orientation="Horizontal">
                <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Open.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Foreground="{StaticResource FontColor}" Text="Browse"/>
              </StackPanel>
            </Button.Content>
          </Button>
          <Label x:Name="BinaryDragTips">
            <Label.Style>
              <Style TargetType="{x:Type Label}">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding BinaryFilePath}" Value="">
                    <Setter Property="Visibility" Value="Visible"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Label.Style>
            <TextBlock Foreground="{StaticResource FontColor}" Text="or drag file here"/>
          </Label>
          <TextBox Padding="2.5" Margin="2" Width="Auto"
                   BorderThickness="1"
                   Text="{Binding BinaryFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsTab="False"
                   AcceptsReturn="False"
                   AutoWordSelection="False"
                   VerticalScrollBarVisibility="Disabled"
                   HorizontalScrollBarVisibility="Disabled">
          </TextBox>
        </WrapPanel>

        <WrapPanel Orientation="Horizontal" Margin="3">
          <Label Target="{Binding ElementName=HistogramBrowseButton}">
            <StackPanel Orientation="Horizontal">
              <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Grid.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
              <AccessText Foreground="{StaticResource FontColor}" Text="_Histogram: "/>
            </StackPanel>
          </Label>
          <Button x:Name="HistogramBrowseButton" Padding="6, 3, 6, 3" Margin="0, 2, 0, 2" Width="Auto" Command="{Binding BrowseCommand}" CommandParameter="Histogram">
            <Button.Content>
              <StackPanel Orientation="Horizontal">
                <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Open.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Foreground="{StaticResource FontColor}" Text="Browse"/>
              </StackPanel>
            </Button.Content>
          </Button>
          <Label x:Name="HistogramDragTips">
            <Label.Style>
              <Style TargetType="{x:Type Label}">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding HistogramFilePath}" Value="">
                    <Setter Property="Visibility" Value="Visible"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Label.Style>
            <Label.Content>
              <TextBlock Foreground="{StaticResource FontColor}" Text="or drag file here"/>
            </Label.Content>
          </Label>
          <TextBox Padding="2.5" Margin="2" Width="Auto"
                   BorderThickness="1"
                   Text="{Binding HistogramFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsTab="False"
                   AcceptsReturn="False"
                   AutoWordSelection="False"
                   VerticalScrollBarVisibility="Disabled"
                   HorizontalScrollBarVisibility="Disabled">
          </TextBox>
        </WrapPanel>

        <WrapPanel Orientation="Horizontal" Margin="3">
          <Label Target="{Binding ElementName=LanguageComboBox}">
            <Label.Content>
              <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0, 1, 5, 0" Text="&#xF2B7;"
                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                <AccessText Foreground="{StaticResource FontColor}" Text="_Language: "/>
              </StackPanel>
            </Label.Content>
          </Label>
          <ComboBox x:Name="LanguageComboBox" MinWidth="140" Width="Auto" Margin="2"
                    ItemsSource="{Binding Languages}"
                    SelectedItem="{Binding SelectedLanguage}">
          </ComboBox>
        </WrapPanel>

        <StackPanel Margin="3" Orientation="Vertical">
          <Label Width="Auto">
            <Label.Content>
              <StackPanel Orientation="Horizontal">
                <Image Margin="0, 2, 5, 0" Width="14" Source="/FrostyCore;component/Images/Settings.png"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Foreground="{StaticResource FontColor}" Text="Options: "/>
              </StackPanel>
            </Label.Content>
          </Label>

          <WrapPanel Orientation="Vertical" Margin="15, 0, 0, 0">
            <CheckBox Margin="3, 0, 3, 0" VerticalContentAlignment="Center"
                      IsChecked="{Binding NoImportEmptyOrNull}" Cursor="Hand">
              <Label>
                <StackPanel Orientation="Horizontal">
                  <AccessText Foreground="{StaticResource FontColor}" Text="Do not import empty or _null strings"/>
                </StackPanel>
              </Label>
            </CheckBox>

            <CheckBox Margin="3, 0, 3, 0" VerticalContentAlignment="Center"
                      IsChecked="{Binding DeleteExistingStrings}" Cursor="Hand">
              <Label>
                <StackPanel Orientation="Horizontal">
                  <AccessText Foreground="{StaticResource FontColor}" Text="Remove all _existing strings"/>
                </StackPanel>
              </Label>
            </CheckBox>

            <CheckBox Margin="3, 0, 3, 0" VerticalContentAlignment="Center"
                      IsChecked="{Binding NoOverwriteSameStrings}" Cursor="Hand">
              <CheckBox.Style>
                <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
                  <Setter Property="IsEnabled" Value="True"/>
                  <Style.Triggers>
                    <DataTrigger Binding="{Binding DeleteExistingStrings}" Value="True">
                      <Setter Property="IsEnabled" Value="False"/>
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </CheckBox.Style>
              <Label>
                <StackPanel Orientation="Horizontal">
                  <AccessText Foreground="{StaticResource FontColor}" Text="Do not _overwrite same existing strings"/>
                </StackPanel>
              </Label>
            </CheckBox>
          </WrapPanel>
        </StackPanel>
      </StackPanel>
    </ScrollViewer>
    <Grid x:Name="NormalFooterGrid" Grid.Row="1" Margin="5">
      <DockPanel LastChildFill="False">
        <Button x:Name="CancelButton" Padding="6, 3, 6, 3" Margin="3" DockPanel.Dock="Left" Width="Auto" Command="{Binding CancelCommand}" IsCancel="True">
          <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0, 1, 5, 0" Text="&#xF3B1;"
                       FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            <AccessText Foreground="{StaticResource FontColor}" Text="_Cancel"/>
          </StackPanel>
        </Button>

        <Button x:Name="ConfirmButton" DockPanel.Dock="Right" Padding="6, 3, 6, 3" Margin="3" Width="Auto" IsDefault="True"
                Command="{Binding ImportCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
          <Button.Content>
            <StackPanel Orientation="Horizontal">
              <Image Margin="0, 1, 5, 0" Width="14" Source="/FrostyEditor;component/Images/Import.png"
                     RenderOptions.BitmapScalingMode="HighQuality"/>
              <AccessText Foreground="{StaticResource FontColor}" Text="_Import"/>
            </StackPanel>
          </Button.Content>
        </Button>
      </DockPanel>
    </Grid>

    <Grid x:Name="DragOverlayGrid" Grid.Row="0" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <Grid x:Name="BinaryDropZone" Grid.Column="0" AllowDrop="True" Drop="BinaryDropZone_Drop" Background="{StaticResource ListBackground}">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Text="Drop here for" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="2" Text="Binary Strings" Foreground="{StaticResource FontColor}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Grid>
      <Border Grid.Column="1" Width="1" Background="{StaticResource ControlBackground}"/>
      <Grid x:Name="HistogramDropZone" Grid.Column="2" AllowDrop="True" Drop="HistogramDropZone_Drop" Background="{StaticResource ListBackground}">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Text="Drop here for" Foreground="{StaticResource FontColor}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock Grid.Row="2" Text="Histogram" Foreground="{StaticResource FontColor}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Grid>
    </Grid>

    <Grid x:Name="DragFooterGrid" Grid.Row="1" Margin="5" Visibility="Collapsed" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <DockPanel LastChildFill="False">
        <Button x:Name="BackFromDragButton" Padding="6, 3, 6, 3" Margin="3" DockPanel.Dock="Left" Width="Auto" Click="BackFromDragButton_Click">
          <Button.Content>
            <StackPanel Orientation="Horizontal">
              <TextBlock Margin="0,1,5,0" Text="&#xE72B;"
                         FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" FontSize="14"
                         RenderOptions.BitmapScalingMode="HighQuality"/>
              <TextBlock Foreground="{StaticResource FontColor}" Text="Back"/>
            </StackPanel>
          </Button.Content>
        </Button>
      </DockPanel>
    </Grid>

  </Grid>
</local:FlammenwerferWindowBase>
