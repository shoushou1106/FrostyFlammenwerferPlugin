<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FsLocalizationPlugin"
    xmlns:controls="clr-namespace:FsLocalizationPlugin.Controls"
    xmlns:views="clr-namespace:FsLocalizationPlugin.Views"
    xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:fdev="FrostyDeveloper">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <!-- Tab shell for the ID Database editor. The real UI is a UserControl. -->
  <Style TargetType="{x:Type controls:IdDatabaseEditor}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:IdDatabaseEditor}">
          <views:IdDatabaseView x:Name="PART_View"/>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Inspiration from: https://github.com/CommunityToolkit/Windows/tree/main/components/Segmented -->
  <Style x:Key="SegmentedItem" TargetType="{x:Type RadioButton}">
    <Setter Property="Foreground"
          Value="{StaticResource FontColor}" />
    <Setter Property="Background"
          Value="{StaticResource ListBackground}" />
    <Setter Property="Padding"
          Value="2" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RadioButton}">
          <Border x:Name="segmentedItemBorder" Background="{TemplateBinding Background}">
            <ContentPresenter x:Name="content"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsMouseOver" Value="True"/>
                <Condition Property="IsChecked" Value="False"/>
              </MultiTrigger.Conditions>
              <Setter TargetName="segmentedItemBorder"
                      Property="Background"
                      Value="{StaticResource ControlPressed}" />
            </MultiTrigger>
            <Trigger Property="IsPressed"
                     Value="True">
              <Setter TargetName="segmentedItemBorder"
                      Property="Background"
                      Value="#353535" />
            </Trigger>
            <Trigger Property="IsChecked"
                     Value="True">
              <Setter TargetName="segmentedItemBorder"
                      Property="Background"
                      Value="{StaticResource ControlBackground}"/>
            </Trigger>
            <Trigger Property="IsEnabled"
                     Value="False">
              <Setter TargetName="content"
                      Property="Opacity"
                      Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
