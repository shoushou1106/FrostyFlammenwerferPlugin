<ctrl:FrostyDockableWindow x:Class="FlammenwerferPlugin.Editor.Windows.ImportStringWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlammenwerferPlugin.Editor.Windows"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        mc:Ignorable="d"
        Title="Import Strings" MinHeight="450" Height="Auto" Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        FontFamily="system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, sans-serif, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol, Noto Color Emoji, BlinkMacSystemFont, Helvetica Neue, Arial, PingFang SC, PingFang TC, PingFang HK, Microsoft Yahei, Microsoft JhengHei, Global User Interface">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource ListBackground}"/>
        <StackPanel Grid.Row="0" Margin="2">
            <WrapPanel Orientation="Horizontal" Margin="2">
                <Label Width="Auto" Content="File: "/>
                <Button Padding="5, 0, 5, 0" Width="Auto" Margin="2" Content="Browse" Click="FileBrowseButton_Click"/>
                <Label MaxWidth="400" Width="Auto" x:Name="FileNameLabel" Content="Or drag a file here" ToolTip=""/> <!-- Show file name if user choose a file -->
            </WrapPanel>

            <WrapPanel Orientation="Horizontal" Margin="2">
                <Label Width="Auto" Content="File Type: "/>
                <ComboBox x:Name="FileTypeComboBox" MinWidth="100" Width="Auto" Margin="2">
                    <ComboBoxItem Name="Auto" Tag="Auto" IsSelected="True"/>
                    <ComboBoxItem Name="CSV" Tag="CSV"/>
                    <ComboBoxItem Name="Json" Tag="Json"/>
                    <ComboBoxItem Name="Excel" Tag="Excel"/>
                </ComboBox>
            </WrapPanel>
            
        </StackPanel>

        <Grid Grid.Row="1" Height="38">
            
        </Grid>
    </Grid>
</ctrl:FrostyDockableWindow>
